# Environment Variables (optional)

# TensorFlow Backend (default: webgpu, fallback: wasm)
# Options: webgpu, wasm, cpu
TF_BACKEND=webgpu

# Hand Detection Settings
MAX_HANDS=2
MODEL_TYPE=full

# Performance Settings
TARGET_FPS=30
ENABLE_PERFORMANCE_MONITORING=true

# Development
DEV_MODE=true
